import 'package:flutter/material.dart';

class ExpenseListBlankPage extends StatelessWidget {
  const ExpenseListBlankPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [],
    );
  }
}
